/* ------------------------------------------------------------------------
 * L'unica unita di traduzione che definisce la tabella dei profili.
 *
 * profile.h e un header a implementazione inclusa: chi lo include normalmente
 * vede solo le dichiarazioni, questo file lo compila con la definizione. Cosi
 * i numeri restano in un file solo — la regola del progetto — senza che la
 * tabella venga duplicata in ogni oggetto che include l'header.
 *
 * Non c'e altro da mettere qui dentro.
 * --------------------------------------------------------------------- */
#define PROFILE_IMPLEMENTATION
#include "profile.h"
